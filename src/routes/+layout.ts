export const prerender = true; //Required for static site generation.
export const trailingSlash = 'never'; //This is the default but may not work with some servers. Determines whether a URL with a trailing slash like "/about/" returns "/about" instead.
